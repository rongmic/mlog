<% @categories.each do |category|%>
  <%= link_to category do %>
    
  <div class="large-3 columns">
    <span class="label secondary"><%= category.name %></span> x <%= category.articles_count %>
    </div>
  <% end %>
<% end %>

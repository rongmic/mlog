<% if @articles.any? %>
<% @articles.each do |article| %>
  <article>
    <header><h2><%= link_to article.title, article %></h2></header>
    <p>
      <%= raw markdown article.content %>
    </p>
    <footer>
      <p>
        <span title="<%= article.created_at %>"><%= date_format article.created_at %></span>
        (
        #<%= link_to article.category.name, article.category %>
        )
        ,
        <i class="foundicon-eyeball"></i><%= article.hitcount.count %>
      </p>
    </footer>
  </article>
<% end %>
<% else %>
  <h3 class="empty">No Articles Found.</h3>
<% end %>
